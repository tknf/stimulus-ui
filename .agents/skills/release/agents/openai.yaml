interface:
  display_name: "Release"
  short_description: "Prepare and publish authorized Stimulus UI releases"
  default_prompt: "Use $release to prepare the requested Stimulus UI version and publish only explicitly authorized releases."
