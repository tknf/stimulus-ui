interface:
  display_name: "Implementation and Verification"
  short_description: "Astra implements confirmed specifications and performs target verification"
  default_prompt: "Use $impl to implement according to the plan, contract, and request, and execute the target verification required for the changes."
policy:
  allow_implicit_invocation: false
